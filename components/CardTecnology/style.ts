import styled from 'styled-components';

export const StyledContainertecnology = styled.div`
    padding: 20px 0px;
    box-shadow: -10px 10px 20px rgba(0, 0, 0, 0.4);
    background-color: rgba(27, 38, 44, 0.4);
    cursor: pointer;

    :hover{
        transform: scale(1.2);
        transition: .7s;
    }

`;
